# Dev profile — run with: --spring.profiles.active=dev  (or SPRING_PROFILES_ACTIVE=dev)
# Re-exposes the OTP code in the API response and logs so you can sign in
# locally without a real SMS gateway. Never enable this in production.
otp.expose-code=true
